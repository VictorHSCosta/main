<nav class="bg-white shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <%= link_to "Market Place", root_path, class: "text-xl font-bold text-indigo-600" %>
        </div>
      </div>

      <div class="flex items-center">
        <% if logged_in? %>
          <div class="flex items-center space-x-4">
            <span class="text-gray-700 text-sm">Olá, <%= current_user.name %></span>
            <%= link_to "Perfil", profile_path, class: "text-gray-700 hover:text-indigo-600 px-3 py-2 rounded-md text-sm font-medium" %>
            <%= button_to "Sair", logout_path, method: :delete, class: "text-gray-700 hover:text-indigo-600 px-3 py-2 rounded-md text-sm font-medium cursor-pointer bg-transparent border-0", form: { class: "inline" } %>
          </div>
        <% else %>
          <div class="flex items-center space-x-4">
            <%= link_to "Entrar", login_path, class: "text-gray-700 hover:text-indigo-600 px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to "Cadastrar", signup_path, class: "bg-indigo-600 text-white hover:bg-indigo-700 px-4 py-2 rounded-md text-sm font-medium" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
